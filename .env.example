# URL de base du backend DLC (dlc-api).
# Laissez cette variable NON définie (ou supprimez-la) pour utiliser les
# données de démonstration en mémoire, sans backend.
# Pour vous connecter au vrai backend, décommentez la ligne suivante :
# VITE_API_URL=http://localhost:3001/api
